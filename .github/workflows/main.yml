name: Maven build with JaCoCo

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Extract branch name
      shell: bash
      run: echo "branch=$(echo ${GITHUB_REF#refs/heads/})" >>$GITHUB_OUTPUT
      id: extract_branch

    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'adopt-openj9'

    - name: Build with Maven
      run: mvn package

    - name: Generate JaCoCo report
      run: mvn jacoco:report


          
    - name: Add coverage to PR
      id: jacoco
      uses: madrapps/jacoco-report@v1.3
      with:
          paths: ${{ github.workspace }}/target/site/jacoco//jacoco.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 40
          min-coverage-changed-files: 60
      
      # Publish Coverage Job Summary  # Optional
    - name: Add Coverage Job Summary
      run: |
         echo "${{ steps.jacoco.outputs.coverage-overall }}" >> $GITHUB_STEP_SUMMARY
         echo "test-overall=90" >>$GITHUB_OUTPUT
         mkdir -p .github/badges
          
      # uploads the coverage-report.md artifact  # Optional
    
    - name: Generate the badge SVG image
      uses: emibcn/badge-action@v2.0.2
      id: badge
      with:
        label: 'Coverage'
        status: ${{ steps.jacoco.outputs.coverage-overall }}
        color: ${{
          steps.jacoco.outputs.test-overall > 90 && 'green'              ||
          steps.jacoco.outputs.test-overall > 80 && 'yellow,green'       ||
          steps.jacoco.outputs.test-overall > 70 && 'yellow'             ||
          steps.jacoco.outputs.test-overall > 60 && 'orange,yellow'      ||
          steps.jacoco.outputs.test-overall > 50 && 'orange'             ||
          steps.jacoco.outputs.test-overall > 40 && 'red,orange'         ||
          steps.jacoco.outputs.test-overall > 30 && 'red,red,orange'     ||
          steps.jacoco.outputs.test-overall > 20 && 'red,red,red,orange' ||
          'red' }}
        path: .github/badges/coverage.svg
        
    - name: Commit badge
      continue-on-error: true
      env:
        BADGE: ${{ steps.coverage.outputs.path }}
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .github/badges/coverage.svg
        git commit -m "Add/Update badge"
        
    - name: Push badge commit
      uses: ad-m/github-push-action@master
      if: ${{ success() }}
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ steps.coverage.outputs.branch }}
